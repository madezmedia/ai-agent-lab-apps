# Local overrides only. Never commit a real key.
# `whop apps dev` injects WHOP_API_KEY for you after `whop auth login`.
# Whop hosting injects the app API key on server fetches to WHOP_API_ORIGIN.
# This app is linked to app_6rPTq6E2yzuohZ in whop.app.json.
#
# WHOP_API_KEY=
